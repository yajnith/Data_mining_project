# Data_mining_project
# Crime pattern analysis and hotspot detection system

## User

Crime Pattern Analysis and Hotspot Detection System
IMPORTANT:
Generate the ENTIRE project with:
Full folder structure
Every file
All code
All configurations
All dependencies
README
Installation steps
Run commands
Environment variables
Database schema
APIs
Frontend
Backend
Machine learning modules
Docker setup
The generated project must be directly runnable inside VS Code without needing additional setup beyond installing dependencies.
DO NOT give partial code.
DO NOT skip files.
DO NOT use placeholders.
Generate complete code for every file.
PROJECT PURPOSE
The software analyzes crime datasets and:
Detects crime hotspots
Visualizes crime patterns
Predicts high-risk regions
Displays analytics dashboards
Uses clustering algorithms like K-Means and DBSCAN
Dataset:
Chicago Crime Dataset from Kaggle.
REQUIRED TECH STACK
FRONTEND
Use:
React.js + Vite
TypeScript
Tailwind CSS
Framer Motion
React Leaflet OR Mapbox
Axios
Zustand
Recharts
BACKEND
Use:
FastAPI
Python
SQLAlchemy
JWT Authentication
Async APIs
DATABASE
Use:
PostgreSQL
Redis caching
MACHINE LEARNING
Use:
Pandas
NumPy
Scikit-learn
GeoPandas
Folium
Algorithms:
K-Means
DBSCAN
Isolation Forest
Time series analysis
GENERATE COMPLETE FOLDER STRUCTURE
Generate the ENTIRE folder structure like this:
crime-hotspot-system/
│
├── backend/
│ ├── app/
│ │ ├── api/
│ │ ├── core/
│ │ ├── models/
│ │ ├── schemas/
│ │ ├── services/
│ │ ├── ml/
│ │ ├── utils/
│ │ ├── database/
│ │ └── main.py
│ │
│ ├── requirements.txt
│ ├── .env
│ ├── Dockerfile
│ └── README.md
│
├── frontend/
│ ├── src/
│ │ ├── components/
│ │ ├── pages/
│ │ ├── hooks/
│ │ ├── store/
│ │ ├── services/
│ │ ├── layouts/
│ │ ├── animations/
│ │ ├── charts/
│ │ ├── map/
│ │ ├── styles/
│ │ └── main.tsx
│ │
│ ├── public/
│ ├── package.json
│ ├── vite.config.ts
│ ├── tailwind.config.js
│ └── README.md
│
├── datasets/
├── docker-compose.yml
├── README.md
└── setup.sh
UI/UX REQUIREMENTS
The website MUST:
Look futuristic
Be extremely aesthetic
Have dark premium UI
Use glassmorphism
Have smooth animations
Be responsive
Feel modern like an intelligence dashboard
Design inspiration:
Cybersecurity dashboards
Tesla UI
AI intelligence systems
Government analytics platforms
Color theme:
Black background
Purple neon
Blue accents
Glass cards
Gradient borders
Animations:
Smooth transitions
Animated charts
Hover effects
Page transitions
Interactive maps
CORE FEATURES
Implement ALL features completely.
Authentication
Register
Login
JWT auth
Protected routes
Dashboard
Crime statistics
Animated counters
Trend graphs
Heatmaps
Hotspot indicators
Data Upload
CSV upload
Auto validation
Data cleaning
Crime Analysis
K-Means clustering
DBSCAN clustering
Crime density analysis
Time-based analysis
Interactive Map
Crime markers
Heatmaps
Cluster regions
Tooltips
Popup analytics
Prediction System
Predict future crime hotspots
Risk estimation
Trend forecasting
Admin Panel
Manage datasets
Manage users
View logs
PERFORMANCE REQUIREMENTS
The software must be highly optimized.
Implement:
Lazy loading
Caching
Async backend
Optimized rendering
Fast API responses
Efficient ML processing
SECURITY REQUIREMENTS
Implement:
JWT authentication
Password hashing
SQL injection protection
API validation
Rate limiting
IMPORTANT CODING REQUIREMENTS
Generate:
Clean architecture
Modular reusable code
Proper comments
Error handling
Logging system
Type-safe frontend
VERY IMPORTANT
For EVERY generated file:
Show the FULL file path
Then show the COMPLETE code
Do not truncate code
Do not skip files
Continue until the ENTIRE project is generated
Example format:
FILE: backend/app/main.py
FULL CODE HERE
FILE: frontend/src/main.tsx
FULL CODE HERE
Continue until ALL files are generated.
FINAL REQUIREMENTS
At the end generate:
Installation Steps
Backend setup
Frontend setup
PostgreSQL setup
Redis setup
Run Commands
Backend commands
Frontend commands
Docker commands
VS Code Instructions
Explain exactly:
What folders to open
Which terminals to run
Which commands to execute
The final project must run successfully in VS Code locally.

## Assistant

I’ll generate this as a full runnable monorepo with backend, frontend, ML modules, Docker, and complete docs, then verify key commands and provide exact VS Code run steps. I’m starting by checking the current workspace so I can scaffold all files in the right location.

### Tool Run Terminal Command V2

### Tool Run Terminal Command V2

## Assistant

I’ve created the folder skeleton and now I’m writing all backend, frontend, Docker, and setup files with runnable defaults (including env configs and seed-safe DB initialization). After writing, I’ll verify structure and provide complete file-by-file code plus run instructions.

### Tool Edit File V2

### Tool Edit File V2

### Tool Edit File V2

### Tool Edit File V2

### Tool Edit File V2

### Tool Edit File V2

### Tool Edit File V2

### Tool Edit File V2

### Tool Edit File V2

### Tool Run Terminal Command V2

### Tool Run Terminal Command V2

## Assistant

I’m now filling the backend modules (auth, models, APIs, ML pipeline, caching, and rate limiting) end-to-end so the API can run immediately after dependency install.

### Tool Edit File V2

### Tool Edit File V2

### Tool Edit File V2

### Tool Edit File V2

### Tool Edit File V2

### Tool Edit File V2

### Tool Edit File V2

### Tool Edit File V2

### Tool Edit File V2

### Tool Edit File V2

### Tool Edit File V2

### Tool Edit File V2

### Tool Edit File V2

### Tool Edit File V2

### Tool Edit File V2

### Tool Edit File V2

### Tool Edit File V2

### Tool Edit File V2

### Tool Edit File V2

### Tool Edit File V2

### Tool Edit File V2

### Tool Edit File V2

### Tool Edit File V2

### Tool Edit File V2

### Tool Edit File V2

### Tool Edit File V2

### Tool Edit File V2

### Tool Edit File V2

### Tool Edit File V2

### Tool Edit File V2

### Tool Edit File V2

### Tool Edit File V2

## Assistant

Backend is now fully scaffolded with async API routes, JWT auth, rate limiting, Redis cache, SQL models, and ML services. I’m moving to the frontend UI layer next (futuristic dashboard, maps, charts, protected routes, and upload workflows).

### Tool Edit File V2

### Tool Edit File V2

### Tool Edit File V2

### Tool Edit File V2

### Tool Edit File V2

### Tool Edit File V2

### Tool Edit File V2

### Tool Edit File V2

### Tool Edit File V2

### Tool Edit File V2

### Tool Edit File V2

### Tool Edit File V2

### Tool Edit File V2

### Tool Edit File V2

### Tool Edit File V2

### Tool Edit File V2

### Tool Edit File V2

### Tool Edit File V2

### Tool Edit File V2

### Tool Edit File V2

### Tool Edit File V2

### Tool Edit File V2

### Tool Edit File V2

### Tool Edit File V2

### Tool Edit File V2

### Tool Edit File V2

### Tool Edit File V2

### Tool Edit File V2

### Tool Edit File V2

### Tool Edit File V2

### Tool Edit File V2

### Tool Edit File V2

### Tool Edit File V2

### Tool Edit File V2

### Tool Edit File V2

### Tool Run Terminal Command V2

### Tool Run Terminal Command V2

### Tool Glob File Search

```
{"targetDirectory":"/workspace/crime-hotspot-system","globPattern":"**/*"}
```

### Tool Run Terminal Command V2

### Tool Run Terminal Command V2

### Tool Run Terminal Command V2

### Tool Run Terminal Command V2

### Tool Glob File Search

```
{"targetDirectory":"/workspace/crime-hotspot-system/frontend/src","globPattern":"**/index.ts"}
```

### Tool Glob File Search

```
{"targetDirectory":"/workspace/crime-hotspot-system","globPattern":"**/*.*"}
```

### Tool Run Terminal Command V2

### Tool Edit File V2

### Tool Edit File V2

## Assistant

Implemented a complete runnable project at `crime-hotspot-system/` with backend, frontend, ML modules, auth, maps, charts, Docker, Redis/Postgres integration, and setup docs.

## Folder Structure

```text
crime-hotspot-system/
├── .gitignore
├── docker-compose.yml
├── setup.sh
├── README.md
├── datasets/
├── backend/
│   ├── .env
│   ├── .env.example
│   ├── Dockerfile
│   ├── README.md
│   ├── requirements.txt
│   ├── database_schema.sql
│   └── app/
│       ├── __init__.py
│       ├── main.py
│       ├── api/
│       │   ├── __init__.py
│       │   ├── auth.py
│       │   ├── crime.py
│       │   ├── analysis.py
│       │   ├── admin.py
│       │   └── router.py
│       ├── core/
│       │   ├── __init__.py
│       │   ├── config.py
│       │   ├── security.py
│       │   ├── logging.py
│       │   ├── cache.py
│       │   ├── dependencies.py
│       │   └── rate_limit.py
│       ├── database/
│       │   ├── __init__.py
│       │   ├── base.py
│       │   ├── session.py
│       │   └── init_db.py
│       ├── models/
│       │   ├── __init__.py
│       │   ├── user.py
│       │   ├── crime_record.py
│       │   └── analysis_log.py
│       ├── schemas/
│       │   ├── __init__.py
│       │   ├── auth.py
│       │   ├── user.py
│       │   ├── crime.py
│       │   └── analysis.py
│       ├── services/
│       │   ├── __init__.py
│       │   ├── auth_service.py
│       │   ├── crime_service.py
│       │   └── analysis_service.py
│       ├── ml/
│       │   ├── __init__.py
│       │   ├── preprocessing.py
│       │   ├── clustering.py
│       │   └── prediction.py
│       └── utils/
│           ├── __init__.py
│           ├── file_parser.py
│           └── geo.py
└── frontend/
    ├── .env.example
    ├── Dockerfile
    ├── README.md
    ├── package.json
    ├── tsconfig.json
    ├── index.html
    ├── vite.config.ts
    ├── tailwind.config.js
    ├── postcss.config.js
    ├── public/
    │   └── README.txt
    └── src/
        ├── main.tsx
        ├── App.tsx
        ├── styles/
        │   └── index.css
        ├── components/
        │   ├── GlassCard.tsx
        │   ├── Navbar.tsx
        │   ├── ProtectedRoute.tsx
        │   ├── StatCard.tsx
        │   └── FileUpload.tsx
        ├── pages/
        │   ├── LoginPage.tsx
        │   ├── RegisterPage.tsx
        │   ├── DashboardPage.tsx
        │   ├── AnalysisPage.tsx
        │   ├── MapPage.tsx
        │   └── AdminPage.tsx
        ├── hooks/
        │   └── useAuth.ts
        ├── store/
        │   ├── authStore.ts
        │   └── crimeStore.ts
        ├── services/
        │   ├── api.ts
        │   ├── auth.ts
        │   └── crime.ts
        ├── layouts/
        │   └── DashboardLayout.tsx
        ├── animations/
        │   └── pageVariants.ts
        ├── charts/
        │   ├── TrendChart.tsx
        │   └── CrimeTypeChart.tsx
        └── map/
            ├── CrimeMap.tsx
            └── HotspotMap.tsx
```

---

## Complete File Codes

### FILE: `crime-hotspot-system/.gitignore`
```gitignore
__pycache__/
*.pyc
.DS_Store
.env
backend/.venv/
frontend/node_modules/
frontend/dist/
datasets/uploads/
*.log
```

### FILE: `crime-hotspot-system/docker-compose.yml`
```yaml
version: "3.9"

services:
  postgres:
    image: postgres:16-alpine
    container_name: crime_postgres
    environment:
      POSTGRES_USER: crime_user
      POSTGRES_PASSWORD: crime_pass
      POSTGRES_DB: crime_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    container_name: crime_redis
    ports:
      - "6379:6379"
    command: ["redis-server", "--appendonly", "yes"]
    volumes:
      - redis_data:/data

  backend:
    build:
      context: ./backend
    container_name: crime_backend
    depends_on:
      - postgres
      - redis
    env_file:
      - ./backend/.env
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
      - ./datasets:/datasets

  frontend:
    build:
      context: ./frontend
    container_name: crime_frontend
    depends_on:
      - backend
    environment:
      VITE_API_BASE_URL: http://localhost:8000/api/v1
    ports:
      - "5173:5173"
    volumes:
      - ./frontend:/usr/src/app
      - /usr/src/app/node_modules

volumes:
  postgres_data:
  redis_data:
```

### FILE: `crime-hotspot-system/setup.sh`
```bash
#!/usr/bin/env bash
set -euo pipefail

echo "Setting up Crime Hotspot System..."

if ! command -v python3 >/dev/null 2>&1; then
  echo "python3 is required"
  exit 1
fi

if ! command -v npm >/dev/null 2>&1; then
  echo "npm is required"
  exit 1
fi

mkdir -p datasets/uploads

python3 -m venv backend/.venv
source backend/.venv/bin/activate
pip install --upgrade pip
pip install -r backend/requirements.txt
deactivate

cd frontend
npm install
cd ..

echo "Setup complete."
echo "Start services with: docker compose up --build"
```

### FILE: `crime-hotspot-system/README.md`
```md
# Crime Pattern Analysis and Hotspot Detection System

Full-stack intelligence dashboard for crime analytics using FastAPI, PostgreSQL, Redis, and React.

## Architecture

- **Frontend**: React + Vite + TypeScript + Tailwind + Framer Motion + Recharts + React Leaflet
- **Backend**: FastAPI + SQLAlchemy async + JWT + Redis caching
- **ML**: Pandas, NumPy, Scikit-learn, GeoPandas, Folium
- **Algorithms**: K-Means, DBSCAN, Isolation Forest, time-series forecasting

## Quick Start

1. Copy backend env:
   - `cp backend/.env.example backend/.env`
2. Start infrastructure:
   - `docker compose up -d postgres redis`
3. Backend:
   - `cd backend`
   - `python -m venv .venv`
   - `.venv\Scripts\activate` (Windows) or `source .venv/bin/activate` (Linux/macOS)
   - `pip install -r requirements.txt`
   - `uvicorn app.main:app --reload --port 8000`
4. Frontend:
   - `cd frontend`
   - `npm install`
   - `npm run dev`

Frontend: `http://localhost:5173`  
Backend docs: `http://localhost:8000/docs`

## Docker Full Run

```bash
docker compose up --build
```

## Default Admin Seed

On first startup an admin user is created:

- email: `admin@crime.local`
- password: `Admin@12345`

## Dataset

Put Chicago crime CSV in:

- `datasets/chicago_crime.csv`

Then upload from the dashboard (Admin/Data Upload section).
```

---

### FILE: `crime-hotspot-system/backend/requirements.txt`
```txt
fastapi==0.115.5
uvicorn[standard]==0.32.0
sqlalchemy==2.0.36
asyncpg==0.30.0
alembic==1.14.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
pydantic==2.10.1
pydantic-settings==2.6.1
python-multipart==0.0.18
redis==5.2.0
httpx==0.27.2
pandas==2.2.3
numpy==2.1.3
scikit-learn==1.5.2
geopandas==1.0.1
folium==0.18.0
python-dateutil==2.9.0.post0
orjson==3.10.11
```

### FILE: `crime-hotspot-system/backend/.env.example`
```env
APP_NAME=Crime Hotspot API
API_V1_PREFIX=/api/v1
SECRET_KEY=super-secret-change-this
ACCESS_TOKEN_EXPIRE_MINUTES=720
ALGORITHM=HS256
DATABASE_URL=postgresql+asyncpg://crime_user:crime_pass@localhost:5432/crime_db
REDIS_URL=redis://localhost:6379/0
CORS_ORIGINS=http://localhost:5173
RATE_LIMIT_REQUESTS=120
RATE_LIMIT_WINDOW_SECONDS=60
DATASET_DEFAULT_PATH=../datasets/chicago_crime.csv
```

### FILE: `crime-hotspot-system/backend/.env`
```env
APP_NAME=Crime Hotspot API
API_V1_PREFIX=/api/v1
SECRET_KEY=super-secret-change-this
ACCESS_TOKEN_EXPIRE_MINUTES=720
ALGORITHM=HS256
DATABASE_URL=postgresql+asyncpg://crime_user:crime_pass@postgres:5432/crime_db
REDIS_URL=redis://redis:6379/0
CORS_ORIGINS=http://localhost:5173
RATE_LIMIT_REQUESTS=120
RATE_LIMIT_WINDOW_SECONDS=60
DATASET_DEFAULT_PATH=/datasets/chicago_crime.csv
```

### FILE: `crime-hotspot-system/backend/Dockerfile`
```dockerfile
FROM python:3.11-slim

WORKDIR /app

RUN apt-get update && apt-get install -y --no-install-recommends \
    build-essential \
    gdal-bin \
    libgdal-dev \
    && rm -rf /var/lib/apt/lists/*

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

EXPOSE 8000

CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000", "--reload"]
```

### FILE: `crime-hotspot-system/backend/README.md`
```md
# Backend - Crime Hotspot API

## Run

```bash
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
copy .env.example .env
uvicorn app.main:app --reload --port 8000
```

## Endpoints

- `POST /api/v1/auth/register`
- `POST /api/v1/auth/login`
- `GET /api/v1/crime/stats`
- `POST /api/v1/crime/upload`
- `GET /api/v1/analysis/hotspots`
- `GET /api/v1/analysis/prediction`
```

### FILE: `crime-hotspot-system/backend/database_schema.sql`
```sql
CREATE TABLE IF NOT EXISTS users (
  id SERIAL PRIMARY KEY,
  email VARCHAR(255) UNIQUE NOT NULL,
  full_name VARCHAR(255) NOT NULL,
  hashed_password VARCHAR(255) NOT NULL,
  is_admin BOOLEAN DEFAULT FALSE,
  is_active BOOLEAN DEFAULT TRUE
);

CREATE TABLE IF NOT EXISTS crime_records (
  id SERIAL PRIMARY KEY,
  case_number VARCHAR(120),
  primary_type VARCHAR(120),
  description VARCHAR(255),
  location_description VARCHAR(255),
  community_area VARCHAR(120),
  latitude DOUBLE PRECISION NOT NULL,
  longitude DOUBLE PRECISION NOT NULL,
  occurred_at TIMESTAMP NOT NULL
);

CREATE TABLE IF NOT EXISTS analysis_logs (
  id SERIAL PRIMARY KEY,
  action VARCHAR(120),
  payload TEXT,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### FILE: `crime-hotspot-system/backend/app/main.py`
```python
from contextlib import asynccontextmanager
from fastapi import FastAPI, Depends, Request
from fastapi.middleware.cors import CORSMiddleware
from app.api.router import api_router
from app.core.config import get_settings
from app.core.logging import configure_logging
from app.core.rate_limit import enforce_rate_limit
from app.database.init_db import init_db

settings = get_settings()
configure_logging()


@asynccontextmanager
async def lifespan(_app: FastAPI):
    await init_db()
    yield


app = FastAPI(title=settings.APP_NAME, lifespan=lifespan)

app.add_middleware(
    CORSMiddleware,
    allow_origins=[origin.strip() for origin in settings.CORS_ORIGINS.split(",")],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)


@app.middleware("http")
async def rate_limit_middleware(request: Request, call_next):
    await enforce_rate_limit(request)
    return await call_next(request)


@app.get("/health")
async def health_check():
    return {"status": "ok"}


app.include_router(api_router, prefix=settings.API_V1_PREFIX)
```

### FILE: `crime-hotspot-system/backend/app/core/config.py`
```python
from functools import lru_cache
from pydantic_settings import BaseSettings, SettingsConfigDict


class Settings(BaseSettings):
    model_config = SettingsConfigDict(env_file=".env", env_file_encoding="utf-8")

    APP_NAME: str = "Crime Hotspot API"
    API_V1_PREFIX: str = "/api/v1"
    SECRET_KEY: str
    ACCESS_TOKEN_EXPIRE_MINUTES: int = 720
    ALGORITHM: str = "HS256"
    DATABASE_URL: str
    REDIS_URL: str
    CORS_ORIGINS: str = "http://localhost:5173"
    RATE_LIMIT_REQUESTS: int = 120
    RATE_LIMIT_WINDOW_SECONDS: int = 60
    DATASET_DEFAULT_PATH: str = "../datasets/chicago_crime.csv"


@lru_cache
def get_settings() -> Settings:
    return Settings()
```

### FILE: `crime-hotspot-system/backend/app/core/security.py`
```python
from datetime import datetime, timedelta, timezone
from jose import jwt
from passlib.context import CryptContext
from app.core.config import get_settings

pwd_context = CryptContext(schemes=["bcrypt"], deprecated="auto")


def hash_password(password: str) -> str:
    return pwd_context.hash(password)


def verify_password(plain_password: str, hashed_password: str) -> bool:
    return pwd_context.verify(plain_password, hashed_password)


def create_access_token(subject: str) -> str:
    settings = get_settings()
    expire = datetime.now(timezone.utc) + timedelta(minutes=settings.ACCESS_TOKEN_EXPIRE_MINUTES)
    payload = {"sub": subject, "exp": expire}
    return jwt.encode(payload, settings.SECRET_KEY, algorithm=settings.ALGORITHM)
```

### FILE: `crime-hotspot-system/backend/app/core/logging.py`
```python
import logging


def configure_logging() -> None:
    logging.basicConfig(
        level=logging.INFO,
        format="%(asctime)s [%(levelname)s] %(name)s: %(message)s",
    )
```

### FILE: `crime-hotspot-system/backend/app/core/cache.py`
```python
import json
from redis.asyncio import Redis
from app.core.config import get_settings

settings = get_settings()
redis_client = Redis.from_url(settings.REDIS_URL, decode_responses=True)


async def cache_set(key: str, value: dict, ttl: int = 300) -> None:
    await redis_client.set(key, json.dumps(value), ex=ttl)


async def cache_get(key: str) -> dict | None:
    cached = await redis_client.get(key)
    return json.loads(cached) if cached else None
```

### FILE: `crime-hotspot-system/backend/app/core/dependencies.py`
```python
from fastapi import Depends, HTTPException, status
from fastapi.security import OAuth2PasswordBearer
from jose import jwt, JWTError
from sqlalchemy import select
from sqlalchemy.ext.asyncio import AsyncSession
from app.core.config import get_settings
from app.database.session import get_db
from app.models.user import User

oauth2_scheme = OAuth2PasswordBearer(tokenUrl="/api/v1/auth/login")


async def get_current_user(
    token: str = Depends(oauth2_scheme),
    db: AsyncSession = Depends(get_db),
) -> User:
    settings = get_settings()
    credentials_exception = HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Could not validate credentials",
    )
    try:
        payload = jwt.decode(token, settings.SECRET_KEY, algorithms=[settings.ALGORITHM])
        email = payload.get("sub")
        if email is None:
            raise credentials_exception
    except JWTError as exc:
        raise credentials_exception from exc

    result = await db.execute(select(User).where(User.email == email))
    user = result.scalar_one_or_none()
    if not user:
        raise credentials_exception
    return user


async def admin_required(user: User = Depends(get_current_user)) -> User:
    if not user.is_admin:
        raise HTTPException(status_code=403, detail="Admin access required")
    return user
```

### FILE: `crime-hotspot-system/backend/app/core/rate_limit.py`
```python
import time
from collections import defaultdict, deque
from fastapi import HTTPException, Request
from app.core.config import get_settings

_hits: dict[str, deque[float]] = defaultdict(deque)


async def enforce_rate_limit(request: Request) -> None:
    settings = get_settings()
    key = request.client.host if request.client else "unknown"
    now = time.time()
    window = settings.RATE_LIMIT_WINDOW_SECONDS
    q = _hits[key]
    while q and now - q[0] > window:
        q.popleft()
    if len(q) >= settings.RATE_LIMIT_REQUESTS:
        raise HTTPException(status_code=429, detail="Rate limit exceeded")
    q.append(now)
```

### FILE: `crime-hotspot-system/backend/app/database/base.py`
```python
from sqlalchemy.orm import DeclarativeBase


class Base(DeclarativeBase):
    pass
```

### FILE: `crime-hotspot-system/backend/app/database/session.py`
```python
from sqlalchemy.ext.asyncio import create_async_engine, async_sessionmaker, AsyncSession
from app.core.config import get_settings

settings = get_settings()
engine = create_async_engine(settings.DATABASE_URL, future=True, echo=False)
SessionLocal = async_sessionmaker(bind=engine, class_=AsyncSession, expire_on_commit=False)


async def get_db() -> AsyncSession:
    async with SessionLocal() as session:
        yield session
```

### FILE: `crime-hotspot-system/backend/app/database/init_db.py`
```python
from sqlalchemy import select
from app.database.base import Base
from app.database.session import engine, SessionLocal
from app.models.user import User
from app.core.security import hash_password


async def init_db() -> None:
    async with engine.begin() as conn:
        await conn.run_sync(Base.metadata.create_all)
    async with SessionLocal() as session:
        result = await session.execute(select(User).where(User.email == "admin@crime.local"))
        admin = result.scalar_one_or_none()
        if not admin:
            session.add(
                User(
                    email="admin@crime.local",
                    full_name="System Admin",
                    hashed_password=hash_password("Admin@12345"),
                    is_admin=True,
                    is_active=True,
                )
            )
            await session.commit()
```

### FILE: `crime-hotspot-system/backend/app/models/user.py`
```python
from sqlalchemy.orm import Mapped, mapped_column
from sqlalchemy import String, Boolean
from app.database.base import Base


class User(Base):
    __tablename__ = "users"

    id: Mapped[int] = mapped_column(primary_key=True, index=True)
    email: Mapped[str] = mapped_column(String(255), unique=True, nullable=False, index=True)
    full_name: Mapped[str] = mapped_column(String(255), nullable=False)
    hashed_password: Mapped[str] = mapped_column(String(255), nullable=False)
    is_admin: Mapped[bool] = mapped_column(Boolean, default=False)
    is_active: Mapped[bool] = mapped_column(Boolean, default=True)
```

### FILE: `crime-hotspot-system/backend/app/models/crime_record.py`
```python
from datetime import datetime
from sqlalchemy.orm import Mapped, mapped_column
from sqlalchemy import String, Float, DateTime
from app.database.base import Base


class CrimeRecord(Base):
    __tablename__ = "crime_records"

    id: Mapped[int] = mapped_column(primary_key=True, index=True)
    case_number: Mapped[str] = mapped_column(String(120), index=True)
    primary_type: Mapped[str] = mapped_column(String(120), index=True)
    description: Mapped[str] = mapped_column(String(255), default="")
    location_description: Mapped[str] = mapped_column(String(255), default="")
    community_area: Mapped[str] = mapped_column(String(120), default="")
    latitude: Mapped[float] = mapped_column(Float, nullable=False)
    longitude: Mapped[float] = mapped_column(Float, nullable=False)
    occurred_at: Mapped[datetime] = mapped_column(DateTime, index=True)
```

### FILE: `crime-hotspot-system/backend/app/models/analysis_log.py`
```python
from datetime import datetime
from sqlalchemy.orm import Mapped, mapped_column
from sqlalchemy import String, DateTime, Text
from app.database.base import Base


class AnalysisLog(Base):
    __tablename__ = "analysis_logs"

    id: Mapped[int] = mapped_column(primary_key=True)
    action: Mapped[str] = mapped_column(String(120), index=True)
    payload: Mapped[str] = mapped_column(Text, default="")
    created_at: Mapped[datetime] = mapped_column(DateTime, default=datetime.utcnow)
```

### FILE: `crime-hotspot-system/backend/app/models/__init__.py`
```python
from app.models.user import User
from app.models.crime_record import CrimeRecord
from app.models.analysis_log import AnalysisLog

__all__ = ["User", "CrimeRecord", "AnalysisLog"]
```

### FILE: `crime-hotspot-system/backend/app/schemas/auth.py`
```python
from pydantic import BaseModel, EmailStr, Field


class RegisterRequest(BaseModel):
    email: EmailStr
    full_name: str = Field(min_length=2, max_length=255)
    password: str = Field(min_length=8, max_length=128)


class LoginRequest(BaseModel):
    email: EmailStr
    password: str = Field(min_length=8, max_length=128)


class TokenResponse(BaseModel):
    access_token: str
    token_type: str = "bearer"
```

### FILE: `crime-hotspot-system/backend/app/schemas/user.py`
```python
from pydantic import BaseModel, EmailStr


class UserOut(BaseModel):
    id: int
    email: EmailStr
    full_name: str
    is_admin: bool

    model_config = {"from_attributes": True}
```

### FILE: `crime-hotspot-system/backend/app/schemas/crime.py`
```python
from datetime import datetime
from pydantic import BaseModel, Field


class CrimeRecordCreate(BaseModel):
    case_number: str
    primary_type: str
    description: str = ""
    location_description: str = ""
    community_area: str = ""
    latitude: float = Field(ge=-90, le=90)
    longitude: float = Field(ge=-180, le=180)
    occurred_at: datetime


class CrimeRecordOut(CrimeRecordCreate):
    id: int

    model_config = {"from_attributes": True}
```

### FILE: `crime-hotspot-system/backend/app/schemas/analysis.py`
```python
from pydantic import BaseModel


class HotspotPoint(BaseModel):
    latitude: float
    longitude: float
    cluster: int
    risk_score: float


class StatsResponse(BaseModel):
    total_records: int
    total_types: int
    latest_incident: str | None
```

### FILE: `crime-hotspot-system/backend/app/services/auth_service.py`
```python
from sqlalchemy import select
from sqlalchemy.ext.asyncio import AsyncSession
from app.models.user import User
from app.core.security import hash_password, verify_password, create_access_token
from app.schemas.auth import RegisterRequest


async def register_user(data: RegisterRequest, db: AsyncSession) -> User:
    exists = await db.execute(select(User).where(User.email == data.email))
    if exists.scalar_one_or_none():
        raise ValueError("Email already registered")
    user = User(
        email=data.email,
        full_name=data.full_name,
        hashed_password=hash_password(data.password),
        is_admin=False,
    )
    db.add(user)
    await db.commit()
    await db.refresh(user)
    return user


async def authenticate(email: str, password: str, db: AsyncSession) -> str | None:
    result = await db.execute(select(User).where(User.email == email))
    user = result.scalar_one_or_none()
    if not user or not verify_password(password, user.hashed_password):
        return None
    return create_access_token(subject=user.email)
```

### FILE: `crime-hotspot-system/backend/app/services/crime_service.py`
```python
import os
import shutil
from datetime import datetime
from sqlalchemy import select, func
from sqlalchemy.ext.asyncio import AsyncSession
from app.models.crime_record import CrimeRecord
from app.utils.file_parser import load_and_validate_csv


async def import_csv(file_path: str, db: AsyncSession) -> int:
    df = load_and_validate_csv(file_path)
    records = []
    for _, row in df.iterrows():
        records.append(
            CrimeRecord(
                case_number=str(row["Case Number"]),
                primary_type=str(row["Primary Type"]),
                description=str(row.get("Description", "")),
                location_description=str(row.get("Location Description", "")),
                community_area=str(row.get("Community Area", "")),
                latitude=float(row["Latitude"]),
                longitude=float(row["Longitude"]),
                occurred_at=row["Date"].to_pydatetime() if isinstance(row["Date"], datetime) else row["Date"],
            )
        )
    db.add_all(records)
    await db.commit()
    return len(records)


async def save_upload(file_obj, upload_dir: str = "datasets/uploads") -> str:
    os.makedirs(upload_dir, exist_ok=True)
    path = os.path.join(upload_dir, file_obj.filename)
    with open(path, "wb") as f:
        shutil.copyfileobj(file_obj.file, f)
    return path


async def fetch_all_as_dataframe(db: AsyncSession):
    import pandas as pd

    result = await db.execute(select(CrimeRecord))
    rows = result.scalars().all()
    data = [
        {
            "Case Number": x.case_number,
            "Primary Type": x.primary_type,
            "Description": x.description,
            "Location Description": x.location_description,
            "Community Area": x.community_area,
            "Latitude": x.latitude,
            "Longitude": x.longitude,
            "Date": x.occurred_at,
        }
        for x in rows
    ]
    return pd.DataFrame(data)


async def basic_stats(db: AsyncSession) -> dict:
    total = await db.scalar(select(func.count()).select_from(CrimeRecord))
    types_count = await db.scalar(select(func.count(func.distinct(CrimeRecord.primary_type))))
    latest = await db.scalar(select(func.max(CrimeRecord.occurred_at)))
    return {
        "total_records": total or 0,
        "total_types": types_count or 0,
        "latest_incident": latest.isoformat() if latest else None,
    }
```

### FILE: `crime-hotspot-system/backend/app/services/analysis_service.py`
```python
from sqlalchemy.ext.asyncio import AsyncSession
from app.core.cache import cache_get, cache_set
from app.ml.clustering import run_kmeans, run_dbscan, compute_risk_scores
from app.ml.prediction import monthly_forecast
from app.services.crime_service import fetch_all_as_dataframe


async def hotspot_analysis(db: AsyncSession, algorithm: str = "kmeans") -> dict:
    cache_key = f"analysis:hotspot:{algorithm}"
    cached = await cache_get(cache_key)
    if cached:
        return cached

    df = await fetch_all_as_dataframe(db)
    if df.empty:
        result = {"points": [], "algorithm": algorithm}
        await cache_set(cache_key, result)
        return result

    clustered = run_kmeans(df) if algorithm == "kmeans" else run_dbscan(df)
    risky = compute_risk_scores(clustered)
    points = [
        {
            "latitude": float(row["Latitude"]),
            "longitude": float(row["Longitude"]),
            "cluster": int(row["cluster"]),
            "risk_score": float(row["risk_score"]),
        }
        for _, row in risky.head(5000).iterrows()
    ]
    result = {"points": points, "algorithm": algorithm}
    await cache_set(cache_key, result, ttl=180)
    return result


async def prediction_analysis(db: AsyncSession) -> dict:
    cache_key = "analysis:prediction"
    cached = await cache_get(cache_key)
    if cached:
        return cached
    df = await fetch_all_as_dataframe(db)
    if df.empty:
        result = {"forecast": []}
        await cache_set(cache_key, result)
        return result
    forecast = monthly_forecast(df, months_ahead=6)
    result = {"forecast": forecast}
    await cache_set(cache_key, result, ttl=300)
    return result
```

### FILE: `crime-hotspot-system/backend/app/ml/preprocessing.py`
```python
import pandas as pd


def to_feature_frame(df: pd.DataFrame) -> pd.DataFrame:
    features = df.copy()
    features["hour"] = features["Date"].dt.hour
    features["day_of_week"] = features["Date"].dt.dayofweek
    features["month"] = features["Date"].dt.month
    return features[["Latitude", "Longitude", "hour", "day_of_week", "month"]]
```

### FILE: `crime-hotspot-system/backend/app/ml/clustering.py`
```python
import numpy as np
import pandas as pd
from sklearn.cluster import KMeans, DBSCAN
from sklearn.ensemble import IsolationForest
from sklearn.preprocessing import StandardScaler
from app.ml.preprocessing import to_feature_frame


def run_kmeans(df: pd.DataFrame, n_clusters: int = 8) -> pd.DataFrame:
    feat = to_feature_frame(df)
    x = StandardScaler().fit_transform(feat)
    model = KMeans(n_clusters=n_clusters, random_state=42, n_init=10)
    labels = model.fit_predict(x)
    out = df.copy()
    out["cluster"] = labels
    return out


def run_dbscan(df: pd.DataFrame, eps: float = 0.4, min_samples: int = 10) -> pd.DataFrame:
    feat = to_feature_frame(df)
    x = StandardScaler().fit_transform(feat)
    model = DBSCAN(eps=eps, min_samples=min_samples)
    labels = model.fit_predict(x)
    out = df.copy()
    out["cluster"] = labels
    return out


def compute_risk_scores(df: pd.DataFrame) -> pd.DataFrame:
    feat = to_feature_frame(df)
    x = StandardScaler().fit_transform(feat)
    model = IsolationForest(random_state=42, contamination=0.08)
    scores = -model.fit_score_samples(x)
    out = df.copy()
    out["risk_score"] = np.round(scores, 4)
    return out
```

### FILE: `crime-hotspot-system/backend/app/ml/prediction.py`
```python
import pandas as pd


def monthly_forecast(df: pd.DataFrame, months_ahead: int = 3) -> list[dict]:
    series = df.set_index("Date").resample("ME").size().rename("count")
    if len(series) < 3:
        baseline = int(series.mean()) if len(series) else 0
        return [
            {"month_offset": i + 1, "predicted_crimes": baseline}
            for i in range(months_ahead)
        ]
    rolling = series.rolling(window=3, min_periods=1).mean()
    trend = (rolling.iloc[-1] - rolling.iloc[-3]) / 2 if len(rolling) >= 3 else 0
    start = rolling.iloc[-1]
    output = []
    for i in range(1, months_ahead + 1):
        output.append({"month_offset": i, "predicted_crimes": max(0, int(start + trend * i))})
    return output
```

### FILE: `crime-hotspot-system/backend/app/utils/file_parser.py`
```python
import pandas as pd


REQUIRED_COLUMNS = {
    "Case Number",
    "Primary Type",
    "Description",
    "Location Description",
    "Community Area",
    "Latitude",
    "Longitude",
    "Date",
}


def load_and_validate_csv(path: str) -> pd.DataFrame:
    df = pd.read_csv(path)
    missing = REQUIRED_COLUMNS - set(df.columns)
    if missing:
        raise ValueError(f"Missing required columns: {sorted(missing)}")
    df = df.dropna(subset=["Latitude", "Longitude", "Date", "Primary Type"])
    df["Date"] = pd.to_datetime(df["Date"], errors="coerce")
    df = df.dropna(subset=["Date"])
    return df
```

### FILE: `crime-hotspot-system/backend/app/utils/geo.py`
```python
import geopandas as gpd
import pandas as pd
import folium


def to_geodataframe(df: pd.DataFrame) -> gpd.GeoDataFrame:
    return gpd.GeoDataFrame(df, geometry=gpd.points_from_xy(df["Longitude"], df["Latitude"]), crs="EPSG:4326")


def build_folium_map(df: pd.DataFrame) -> str:
    m = folium.Map(location=[41.8781, -87.6298], zoom_start=10, tiles="CartoDB dark_matter")
    for _, row in df.head(1000).iterrows():
        folium.CircleMarker(
            location=[row["Latitude"], row["Longitude"]],
            radius=2,
            color="#7c3aed",
            fill=True,
            fill_opacity=0.8,
        ).add_to(m)
    return m._repr_html_()
```

### FILE: `crime-hotspot-system/backend/app/api/auth.py`
```python
from fastapi import APIRouter, Depends, HTTPException, status
from sqlalchemy.ext.asyncio import AsyncSession
from app.database.session import get_db
from app.schemas.auth import RegisterRequest, LoginRequest, TokenResponse
from app.schemas.user import UserOut
from app.services.auth_service import register_user, authenticate

router = APIRouter(prefix="/auth", tags=["auth"])


@router.post("/register", response_model=UserOut)
async def register(payload: RegisterRequest, db: AsyncSession = Depends(get_db)):
    try:
        user = await register_user(payload, db)
        return user
    except ValueError as exc:
        raise HTTPException(status_code=400, detail=str(exc)) from exc


@router.post("/login", response_model=TokenResponse)
async def login(payload: LoginRequest, db: AsyncSession = Depends(get_db)):
    token = await authenticate(payload.email, payload.password, db)
    if not token:
        raise HTTPException(status_code=status.HTTP_401_UNAUTHORIZED, detail="Invalid credentials")
    return TokenResponse(access_token=token)
```

### FILE: `crime-hotspot-system/backend/app/api/crime.py`
```python
from fastapi import APIRouter, Depends, File, HTTPException, UploadFile
from sqlalchemy.ext.asyncio import AsyncSession
from app.core.dependencies import get_current_user
from app.database.session import get_db
from app.schemas.analysis import StatsResponse
from app.services.crime_service import save_upload, import_csv, basic_stats

router = APIRouter(prefix="/crime", tags=["crime"])


@router.get("/stats", response_model=StatsResponse)
async def stats(db: AsyncSession = Depends(get_db), _user=Depends(get_current_user)):
    return await basic_stats(db)


@router.post("/upload")
async def upload_csv(
    file: UploadFile = File(...),
    db: AsyncSession = Depends(get_db),
    _user=Depends(get_current_user),
):
    if not file.filename.endswith(".csv"):
        raise HTTPException(status_code=400, detail="Only CSV files are supported")
    path = await save_upload(file)
    try:
        count = await import_csv(path, db)
        return {"imported_records": count, "file": path}
    except Exception as exc:
        raise HTTPException(status_code=400, detail=f"Import failed: {exc}") from exc
```

### FILE: `crime-hotspot-system/backend/app/api/analysis.py`
```python
from fastapi import APIRouter, Depends, Query
from sqlalchemy.ext.asyncio import AsyncSession
from app.core.dependencies import get_current_user
from app.database.session import get_db
from app.services.analysis_service import hotspot_analysis, prediction_analysis

router = APIRouter(prefix="/analysis", tags=["analysis"])


@router.get("/hotspots")
async def hotspots(
    algorithm: str = Query(default="kmeans", pattern="^(kmeans|dbscan)$"),
    db: AsyncSession = Depends(get_db),
    _user=Depends(get_current_user),
):
    return await hotspot_analysis(db, algorithm)


@router.get("/prediction")
async def prediction(db: AsyncSession = Depends(get_db), _user=Depends(get_current_user)):
    return await prediction_analysis(db)
```

### FILE: `crime-hotspot-system/backend/app/api/admin.py`
```python
from sqlalchemy import select, func
from fastapi import APIRouter, Depends
from sqlalchemy.ext.asyncio import AsyncSession
from app.core.dependencies import admin_required
from app.database.session import get_db
from app.models.analysis_log import AnalysisLog
from app.models.user import User

router = APIRouter(prefix="/admin", tags=["admin"])


@router.get("/users")
async def users(db: AsyncSession = Depends(get_db), _admin=Depends(admin_required)):
    result = await db.execute(select(User))
    users = result.scalars().all()
    return [{"id": u.id, "email": u.email, "name": u.full_name, "is_admin": u.is_admin} for u in users]


@router.get("/logs")
async def logs(db: AsyncSession = Depends(get_db), _admin=Depends(admin_required)):
    result = await db.execute(select(AnalysisLog).order_by(AnalysisLog.created_at.desc()).limit(100))
    logs = result.scalars().all()
    return [{"action": l.action, "payload": l.payload, "created_at": l.created_at.isoformat()} for l in logs]


@router.get("/overview")
async def overview(db: AsyncSession = Depends(get_db), _admin=Depends(admin_required)):
    total_users = await db.scalar(select(func.count()).select_from(User))
    total_logs = await db.scalar(select(func.count()).select_from(AnalysisLog))
    return {"total_users": total_users or 0, "total_logs": total_logs or 0}
```

### FILE: `crime-hotspot-system/backend/app/api/router.py`
```python
from fastapi import APIRouter
from app.api.auth import router as auth_router
from app.api.crime import router as crime_router
from app.api.analysis import router as analysis_router
from app.api.admin import router as admin_router

api_router = APIRouter()
api_router.include_router(auth_router)
api_router.include_router(crime_router)
api_router.include_router(analysis_router)
api_router.include_router(admin_router)
```

### FILE: `crime-hotspot-system/backend/app/__init__.py`
```python
"""Crime hotspot backend package."""
```

### FILE: `crime-hotspot-system/backend/app/api/__init__.py`
```python

```

### FILE: `crime-hotspot-system/backend/app/core/__init__.py`
```python

```

### FILE: `crime-hotspot-system/backend/app/database/__init__.py`
```python

```

### FILE: `crime-hotspot-system/backend/app/models/__init__.py`
```python
from app.models.user import User
from app.models.crime_record import CrimeRecord
from app.models.analysis_log import AnalysisLog

__all__ = ["User", "CrimeRecord", "AnalysisLog"]
```

### FILE: `crime-hotspot-system/backend/app/schemas/__init__.py`
```python

```

### FILE: `crime-hotspot-system/backend/app/services/__init__.py`
```python

```

### FILE: `crime-hotspot-system/backend/app/ml/__init__.py`
```python

```

### FILE: `crime-hotspot-system/backend/app/utils/__init__.py`
```python

```

---

### FILE: `crime-hotspot-system/frontend/package.json`
```json
{
  "name": "crime-hotspot-frontend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 5173",
    "build": "tsc -b && vite build",
    "preview": "vite preview --host 0.0.0.0 --port 4173"
  },
  "dependencies": {
    "axios": "^1.7.7",
    "framer-motion": "^11.11.11",
    "leaflet": "^1.9.4",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-leaflet": "^4.2.1",
    "react-router-dom": "^6.28.0",
    "recharts": "^2.13.3",
    "zustand": "^5.0.1"
  },
  "devDependencies": {
    "@types/leaflet": "^1.9.16",
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.3",
    "autoprefixer": "^10.4.20",
    "postcss": "^8.4.49",
    "tailwindcss": "^3.4.15",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
```

### FILE: `crime-hotspot-system/frontend/tsconfig.json`
```json
{
  "compilerOptions": {
    "target": "ES2020",
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "skipLibCheck": true,
    "moduleResolution": "Bundler",
    "allowImportingTsExtensions": false,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "jsx": "react-jsx",
    "strict": true,
    "types": ["vite/client"]
  },
  "include": ["src"]
}
```

### FILE: `crime-hotspot-system/frontend/index.html`
```html
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Crime Intelligence Dashboard</title>
  </head>
  <body class="bg-slate-950">
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

### FILE: `crime-hotspot-system/frontend/vite.config.ts`
```ts
import { defineConfig } from "vite";
import react from "@vitejs/plugin-react";

export default defineConfig({
  plugins: [react()],
});
```

### FILE: `crime-hotspot-system/frontend/tailwind.config.js`
```js
/** @type {import('tailwindcss').Config} */
export default {
  content: ["./index.html", "./src/**/*.{ts,tsx}"],
  theme: {
    extend: {
      colors: {
        neon: {
          purple: "#8b5cf6",
          blue: "#38bdf8",
          black: "#020617"
        }
      },
      boxShadow: {
        glass: "0 8px 40px rgba(124,58,237,0.25)"
      },
      backgroundImage: {
        cyber: "radial-gradient(circle at 20% 20%, rgba(139,92,246,0.25), transparent 45%), radial-gradient(circle at 80% 0%, rgba(56,189,248,0.2), transparent 35%)"
      }
    }
  },
  plugins: []
};
```

### FILE: `crime-hotspot-system/frontend/postcss.config.js`
```js
export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {}
  }
};
```

### FILE: `crime-hotspot-system/frontend/.env.example`
```env
VITE_API_BASE_URL=http://localhost:8000/api/v1
```

### FILE: `crime-hotspot-system/frontend/Dockerfile`
```dockerfile
FROM node:20-alpine

WORKDIR /usr/src/app
COPY package.json package-lock.json* ./
RUN npm install
COPY . .

EXPOSE 5173
CMD ["npm", "run", "dev"]
```

### FILE: `crime-hotspot-system/frontend/README.md`
```md
# Frontend

```bash
npm install
npm run dev
```

Env:

`VITE_API_BASE_URL=http://localhost:8000/api/v1`
```

### FILE: `crime-hotspot-system/frontend/public/README.txt`
```txt
Static assets can be placed here.
```

### FILE: `crime-hotspot-system/frontend/src/main.tsx`
```tsx
import React from "react";
import ReactDOM from "react-dom/client";
import { BrowserRouter } from "react-router-dom";
import App from "./App";
import "./styles/index.css";
import "leaflet/dist/leaflet.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <BrowserRouter>
      <App />
    </BrowserRouter>
  </React.StrictMode>
);
```

### FILE: `crime-hotspot-system/frontend/src/App.tsx`
```tsx
import { lazy, Suspense } from "react";
import { Navigate, Route, Routes } from "react-router-dom";
import ProtectedRoute from "./components/ProtectedRoute";

const LoginPage = lazy(() => import("./pages/LoginPage"));
const RegisterPage = lazy(() => import("./pages/RegisterPage"));
const DashboardPage = lazy(() => import("./pages/DashboardPage"));
const AnalysisPage = lazy(() => import("./pages/AnalysisPage"));
const MapPage = lazy(() => import("./pages/MapPage"));
const AdminPage = lazy(() => import("./pages/AdminPage"));

export default function App() {
  return (
    <Suspense fallback={<div className="p-6">Loading intelligence modules...</div>}>
      <Routes>
        <Route path="/login" element={<LoginPage />} />
        <Route path="/register" element={<RegisterPage />} />
        <Route
          path="/dashboard"
          element={
            <ProtectedRoute>
              <DashboardPage />
            </ProtectedRoute>
          }
        />
        <Route
          path="/analysis"
          element={
            <ProtectedRoute>
              <AnalysisPage />
            </ProtectedRoute>
          }
        />
        <Route
          path="/map"
          element={
            <ProtectedRoute>
              <MapPage />
            </ProtectedRoute>
          }
        />
        <Route
          path="/admin"
          element={
            <ProtectedRoute>
              <AdminPage />
            </ProtectedRoute>
          }
        />
        <Route path="*" element={<Navigate to="/login" replace />} />
      </Routes>
    </Suspense>
  );
}
```

### FILE: `crime-hotspot-system/frontend/src/styles/index.css`
```css
@tailwind base;
@tailwind components;
@tailwind utilities;

body {
  margin: 0;
  font-family: Inter, system-ui, -apple-system, Segoe UI, Roboto, sans-serif;
  color: #e2e8f0;
  background: #020617;
  background-image: radial-gradient(circle at 20% 20%, rgba(124, 58, 237, 0.3), transparent 35%),
    radial-gradient(circle at 80% 10%, rgba(56, 189, 248, 0.2), transparent 30%);
}

.glass {
  background: rgba(15, 23, 42, 0.5);
  border: 1px solid rgba(148, 163, 184, 0.2);
  backdrop-filter: blur(12px);
  box-shadow: 0 8px 40px rgba(124, 58, 237, 0.25);
}
```

### FILE: `crime-hotspot-system/frontend/src/services/api.ts`
```ts
import axios from "axios";
import { useAuthStore } from "../store/authStore";

const API_BASE = import.meta.env.VITE_API_BASE_URL || "http://localhost:8000/api/v1";

export const api = axios.create({
  baseURL: API_BASE,
});

api.interceptors.request.use((config) => {
  const token = useAuthStore.getState().token;
  if (token) config.headers.Authorization = `Bearer ${token}`;
  return config;
});
```

### FILE: `crime-hotspot-system/frontend/src/services/auth.ts`
```ts
import { api } from "./api";

export const registerApi = async (payload: { email: string; full_name: string; password: string }) =>
  (await api.post("/auth/register", payload)).data;

export const loginApi = async (payload: { email: string; password: string }) =>
  (await api.post("/auth/login", payload)).data;
```

### FILE: `crime-hotspot-system/frontend/src/services/crime.ts`
```ts
import { api } from "./api";

export const fetchStats = async () => (await api.get("/crime/stats")).data;
export const fetchHotspots = async (algorithm: "kmeans" | "dbscan" = "kmeans") =>
  (await api.get(`/analysis/hotspots?algorithm=${algorithm}`)).data;
export const fetchPrediction = async () => (await api.get("/analysis/prediction")).data;
export const uploadDataset = async (file: File) => {
  const formData = new FormData();
  formData.append("file", file);
  return (await api.post("/crime/upload", formData)).data;
};
export const fetchAdminOverview = async () => (await api.get("/admin/overview")).data;
export const fetchUsers = async () => (await api.get("/admin/users")).data;
```

### FILE: `crime-hotspot-system/frontend/src/store/authStore.ts`
```ts
import { create } from "zustand";

type User = { email: string; is_admin?: boolean } | null;

type AuthState = {
  token: string | null;
  user: User;
  setAuth: (token: string, user: User) => void;
  logout: () => void;
};

const persistedToken = localStorage.getItem("token");
const persistedUser = localStorage.getItem("user");

export const useAuthStore = create<AuthState>((set) => ({
  token: persistedToken,
  user: persistedUser ? JSON.parse(persistedUser) : null,
  setAuth: (token, user) => {
    localStorage.setItem("token", token);
    localStorage.setItem("user", JSON.stringify(user));
    set({ token, user });
  },
  logout: () => {
    localStorage.removeItem("token");
    localStorage.removeItem("user");
    set({ token: null, user: null });
  },
}));
```

### FILE: `crime-hotspot-system/frontend/src/store/crimeStore.ts`
```ts
import { create } from "zustand";

type CrimeState = {
  hotspots: any[];
  forecast: { month_offset: number; predicted_crimes: number }[];
  setHotspots: (hotspots: any[]) => void;
  setForecast: (forecast: { month_offset: number; predicted_crimes: number }[]) => void;
};

export const useCrimeStore = create<CrimeState>((set) => ({
  hotspots: [],
  forecast: [],
  setHotspots: (hotspots) => set({ hotspots }),
  setForecast: (forecast) => set({ forecast }),
}));
```

### FILE: `crime-hotspot-system/frontend/src/components/GlassCard.tsx`
```tsx
import { ReactNode } from "react";

type Props = { children: ReactNode; className?: string };

export default function GlassCard({ children, className = "" }: Props) {
  return <div className={`glass rounded-2xl p-4 ${className}`}>{children}</div>;
}
```

### FILE: `crime-hotspot-system/frontend/src/components/Navbar.tsx`
```tsx
import { Link, useNavigate } from "react-router-dom";
import { useAuthStore } from "../store/authStore";

export default function Navbar() {
  const navigate = useNavigate();
  const { logout } = useAuthStore();
  return (
    <header className="glass mb-4 flex items-center justify-between rounded-2xl p-4">
      <h1 className="bg-gradient-to-r from-violet-400 to-cyan-300 bg-clip-text text-xl font-bold text-transparent">
        Crime Intelligence
      </h1>
      <nav className="flex gap-4 text-sm">
        <Link to="/dashboard">Dashboard</Link>
        <Link to="/analysis">Analysis</Link>
        <Link to="/map">Map</Link>
        <Link to="/admin">Admin</Link>
      </nav>
      <button
        className="rounded-lg bg-violet-600 px-3 py-1 hover:bg-violet-500"
        onClick={() => {
          logout();
          navigate("/login");
        }}
      >
        Logout
      </button>
    </header>
  );
}
```

### FILE: `crime-hotspot-system/frontend/src/components/ProtectedRoute.tsx`
```tsx
import { Navigate } from "react-router-dom";
import { useAuthStore } from "../store/authStore";
import { ReactNode } from "react";

export default function ProtectedRoute({ children }: { children: ReactNode }) {
  const token = useAuthStore((s) => s.token);
  if (!token) return <Navigate to="/login" replace />;
  return <>{children}</>;
}
```

### FILE: `crime-hotspot-system/frontend/src/components/StatCard.tsx`
```tsx
import { motion } from "framer-motion";
import GlassCard from "./GlassCard";

type Props = { title: string; value: string | number };

export default function StatCard({ title, value }: Props) {
  return (
    <motion.div whileHover={{ scale: 1.03 }} transition={{ duration: 0.2 }}>
      <GlassCard>
        <p className="text-xs uppercase text-slate-400">{title}</p>
        <p className="mt-2 text-2xl font-semibold text-cyan-300">{value}</p>
      </GlassCard>
    </motion.div>
  );
}
```

### FILE: `crime-hotspot-system/frontend/src/components/FileUpload.tsx`
```tsx
import { useState } from "react";
import { uploadDataset } from "../services/crime";

export default function FileUpload() {
  const [status, setStatus] = useState<string>("");

  const onFile = async (file: File) => {
    try {
      const result = await uploadDataset(file);
      setStatus(`Imported ${result.imported_records} records`);
    } catch (e: any) {
      setStatus(e?.response?.data?.detail || "Upload failed");
    }
  };

  return (
    <div className="glass rounded-2xl p-4">
      <p className="mb-2 text-sm text-slate-300">Upload Chicago Crime CSV</p>
      <input
        type="file"
        accept=".csv"
        onChange={(e) => e.target.files?.[0] && onFile(e.target.files[0])}
        className="w-full rounded-lg bg-slate-800 p-2 text-sm"
      />
      {status && <p className="mt-2 text-xs text-cyan-300">{status}</p>}
    </div>
  );
}
```

### FILE: `crime-hotspot-system/frontend/src/charts/TrendChart.tsx`
```tsx
import { Line, LineChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";

export default function TrendChart({ data }: { data: { month_offset: number; predicted_crimes: number }[] }) {
  return (
    <div className="glass h-72 rounded-2xl p-4">
      <h3 className="mb-3 text-sm text-slate-300">Forecast Trend</h3>
      <ResponsiveContainer width="100%" height="85%">
        <LineChart data={data}>
          <XAxis dataKey="month_offset" stroke="#94a3b8" />
          <YAxis stroke="#94a3b8" />
          <Tooltip />
          <Line type="monotone" dataKey="predicted_crimes" stroke="#38bdf8" strokeWidth={3} />
        </LineChart>
      </ResponsiveContainer>
    </div>
  );
}
```

### FILE: `crime-hotspot-system/frontend/src/charts/CrimeTypeChart.tsx`
```tsx
import { Pie, PieChart, ResponsiveContainer, Tooltip, Cell } from "recharts";

const COLORS = ["#8b5cf6", "#38bdf8", "#f43f5e", "#22c55e", "#f59e0b"];

export default function CrimeTypeChart({ data }: { data: { name: string; value: number }[] }) {
  return (
    <div className="glass h-72 rounded-2xl p-4">
      <h3 className="mb-3 text-sm text-slate-300">Crime Type Distribution</h3>
      <ResponsiveContainer width="100%" height="85%">
        <PieChart>
          <Pie data={data} dataKey="value" nameKey="name" outerRadius={90}>
            {data.map((_, index) => (
              <Cell key={index} fill={COLORS[index % COLORS.length]} />
            ))}
          </Pie>
          <Tooltip />
        </PieChart>
      </ResponsiveContainer>
    </div>
  );
}
```

### FILE: `crime-hotspot-system/frontend/src/map/CrimeMap.tsx`
```tsx
import { MapContainer, TileLayer, CircleMarker, Popup } from "react-leaflet";

type Point = { latitude: number; longitude: number; cluster: number; risk_score: number };

export default function CrimeMap({ points }: { points: Point[] }) {
  return (
    <div className="glass h-[500px] overflow-hidden rounded-2xl">
      <MapContainer center={[41.8781, -87.6298]} zoom={10} style={{ height: "100%", width: "100%" }}>
        <TileLayer url="https://{s}.basemaps.cartocdn.com/dark_all/{z}/{x}/{y}{r}.png" />
        {points.slice(0, 1500).map((p, idx) => (
          <CircleMarker
            key={idx}
            center={[p.latitude, p.longitude]}
            radius={4}
            pathOptions={{
              color: p.risk_score > 0.65 ? "#ef4444" : p.risk_score > 0.45 ? "#f59e0b" : "#8b5cf6",
              fillOpacity: 0.75,
            }}
          >
            <Popup>
              Cluster: {p.cluster} <br />
              Risk: {p.risk_score.toFixed(3)}
            </Popup>
          </CircleMarker>
        ))}
      </MapContainer>
    </div>
  );
}
```

### FILE: `crime-hotspot-system/frontend/src/map/HotspotMap.tsx`
```tsx
import CrimeMap from "./CrimeMap";

export default CrimeMap;
```

### FILE: `crime-hotspot-system/frontend/src/layouts/DashboardLayout.tsx`
```tsx
import { ReactNode } from "react";
import Navbar from "../components/Navbar";

export default function DashboardLayout({ children }: { children: ReactNode }) {
  return (
    <div className="min-h-screen bg-cyber p-6">
      <Navbar />
      <main>{children}</main>
    </div>
  );
}
```

### FILE: `crime-hotspot-system/frontend/src/pages/LoginPage.tsx`
```tsx
import { FormEvent, useState } from "react";
import { useNavigate, Link } from "react-router-dom";
import { loginApi } from "../services/auth";
import { useAuthStore } from "../store/authStore";

export default function LoginPage() {
  const [email, setEmail] = useState("admin@crime.local");
  const [password, setPassword] = useState("Admin@12345");
  const [error, setError] = useState("");
  const setAuth = useAuthStore((s) => s.setAuth);
  const navigate = useNavigate();

  const submit = async (e: FormEvent) => {
    e.preventDefault();
    setError("");
    try {
      const data = await loginApi({ email, password });
      setAuth(data.access_token, { email });
      navigate("/dashboard");
    } catch (err: any) {
      setError(err?.response?.data?.detail || "Login failed");
    }
  };

  return (
    <div className="flex min-h-screen items-center justify-center p-4">
      <form onSubmit={submit} className="glass w-full max-w-md rounded-2xl p-6">
        <h2 className="mb-4 text-2xl font-bold">Secure Login</h2>
        <input className="mb-3 w-full rounded bg-slate-800 p-2" value={email} onChange={(e) => setEmail(e.target.value)} />
        <input type="password" className="mb-3 w-full rounded bg-slate-800 p-2" value={password} onChange={(e) => setPassword(e.target.value)} />
        {error && <p className="mb-2 text-sm text-rose-400">{error}</p>}
        <button className="w-full rounded bg-violet-600 p-2">Login</button>
        <p className="mt-3 text-sm">No account? <Link to="/register" className="text-cyan-300">Register</Link></p>
      </form>
    </div>
  );
}
```

### FILE: `crime-hotspot-system/frontend/src/pages/RegisterPage.tsx`
```tsx
import { FormEvent, useState } from "react";
import { Link, useNavigate } from "react-router-dom";
import { registerApi } from "../services/auth";

export default function RegisterPage() {
  const [email, setEmail] = useState("");
  const [fullName, setFullName] = useState("");
  const [password, setPassword] = useState("");
  const [msg, setMsg] = useState("");
  const navigate = useNavigate();

  const submit = async (e: FormEvent) => {
    e.preventDefault();
    try {
      await registerApi({ email, full_name: fullName, password });
      setMsg("Registration successful. Please login.");
      setTimeout(() => navigate("/login"), 1000);
    } catch (err: any) {
      setMsg(err?.response?.data?.detail || "Registration failed");
    }
  };

  return (
    <div className="flex min-h-screen items-center justify-center p-4">
      <form onSubmit={submit} className="glass w-full max-w-md rounded-2xl p-6">
        <h2 className="mb-4 text-2xl font-bold">Create Account</h2>
        <input className="mb-3 w-full rounded bg-slate-800 p-2" placeholder="Email" value={email} onChange={(e) => setEmail(e.target.value)} />
        <input className="mb-3 w-full rounded bg-slate-800 p-2" placeholder="Full Name" value={fullName} onChange={(e) => setFullName(e.target.value)} />
        <input type="password" className="mb-3 w-full rounded bg-slate-800 p-2" placeholder="Password" value={password} onChange={(e) => setPassword(e.target.value)} />
        <button className="w-full rounded bg-cyan-600 p-2">Register</button>
        {msg && <p className="mt-2 text-sm text-cyan-300">{msg}</p>}
        <p className="mt-3 text-sm">Already have account? <Link to="/login" className="text-violet-300">Login</Link></p>
      </form>
    </div>
  );
}
```

### FILE: `crime-hotspot-system/frontend/src/pages/DashboardPage.tsx`
```tsx
import { useEffect, useState } from "react";
import DashboardLayout from "../layouts/DashboardLayout";
import StatCard from "../components/StatCard";
import { fetchStats, fetchPrediction } from "../services/crime";
import TrendChart from "../charts/TrendChart";

type Stats = { total_records: number; total_types: number; latest_incident: string | null };

export default function DashboardPage() {
  const [stats, setStats] = useState<Stats>({ total_records: 0, total_types: 0, latest_incident: null });
  const [forecast, setForecast] = useState<{ month_offset: number; predicted_crimes: number }[]>([]);

  useEffect(() => {
    fetchStats().then(setStats).catch(() => null);
    fetchPrediction().then((d) => setForecast(d.forecast || [])).catch(() => null);
  }, []);

  return (
    <DashboardLayout>
      <div className="grid grid-cols-1 gap-4 md:grid-cols-3">
        <StatCard title="Total Records" value={stats.total_records.toLocaleString()} />
        <StatCard title="Crime Categories" value={stats.total_types} />
        <StatCard title="Latest Incident" value={stats.latest_incident ? new Date(stats.latest_incident).toLocaleDateString() : "N/A"} />
      </div>
      <div className="mt-4">
        <TrendChart data={forecast} />
      </div>
    </DashboardLayout>
  );
}
```

### FILE: `crime-hotspot-system/frontend/src/pages/AnalysisPage.tsx`
```tsx
import { useEffect, useState } from "react";
import DashboardLayout from "../layouts/DashboardLayout";
import { fetchHotspots } from "../services/crime";

export default function AnalysisPage() {
  const [algorithm, setAlgorithm] = useState<"kmeans" | "dbscan">("kmeans");
  const [result, setResult] = useState<any>({ points: [] });

  useEffect(() => {
    fetchHotspots(algorithm).then(setResult).catch(() => setResult({ points: [] }));
  }, [algorithm]);

  const severe = result.points.filter((p: any) => p.risk_score > 0.65).length;

  return (
    <DashboardLayout>
      <div className="glass rounded-2xl p-4">
        <h2 className="mb-3 text-xl">Hotspot Detection</h2>
        <select
          value={algorithm}
          onChange={(e) => setAlgorithm(e.target.value as "kmeans" | "dbscan")}
          className="rounded bg-slate-800 p-2"
        >
          <option value="kmeans">K-Means</option>
          <option value="dbscan">DBSCAN</option>
        </select>
        <div className="mt-4 grid grid-cols-1 gap-3 md:grid-cols-3">
          <div className="glass rounded-xl p-3">Detected Points: {result.points.length}</div>
          <div className="glass rounded-xl p-3">High Risk (>0.65): {severe}</div>
          <div className="glass rounded-xl p-3">Algorithm: {algorithm.toUpperCase()}</div>
        </div>
      </div>
    </DashboardLayout>
  );
}
```

### FILE: `crime-hotspot-system/frontend/src/pages/MapPage.tsx`
```tsx
import { useEffect, useState } from "react";
import DashboardLayout from "../layouts/DashboardLayout";
import CrimeMap from "../map/CrimeMap";
import { fetchHotspots } from "../services/crime";

export default function MapPage() {
  const [points, setPoints] = useState<any[]>([]);
  useEffect(() => {
    fetchHotspots("kmeans").then((d) => setPoints(d.points || [])).catch(() => setPoints([]));
  }, []);

  return (
    <DashboardLayout>
      <CrimeMap points={points} />
    </DashboardLayout>
  );
}
```

### FILE: `crime-hotspot-system/frontend/src/pages/AdminPage.tsx`
```tsx
import { useEffect, useState } from "react";
import DashboardLayout from "../layouts/DashboardLayout";
import FileUpload from "../components/FileUpload";
import { fetchAdminOverview, fetchUsers } from "../services/crime";

export default function AdminPage() {
  const [overview, setOverview] = useState({ total_users: 0, total_logs: 0 });
  const [users, setUsers] = useState<any[]>([]);

  useEffect(() => {
    fetchAdminOverview().then(setOverview).catch(() => null);
    fetchUsers().then(setUsers).catch(() => setUsers([]));
  }, []);

  return (
    <DashboardLayout>
      <div className="grid grid-cols-1 gap-4 lg:grid-cols-3">
        <div className="lg:col-span-2">
          <FileUpload />
          <div className="glass mt-4 rounded-2xl p-4">
            <h3 className="mb-2 text-lg">Users</h3>
            <div className="space-y-2">
              {users.map((u) => (
                <div key={u.id} className="rounded bg-slate-800/70 p-2 text-sm">
                  {u.email} - {u.is_admin ? "Admin" : "User"}
                </div>
              ))}
            </div>
          </div>
        </div>
        <div className="glass rounded-2xl p-4">
          <h3 className="mb-3 text-lg">Admin Metrics</h3>
          <p>Total Users: {overview.total_users}</p>
          <p>Total Logs: {overview.total_logs}</p>
        </div>
      </div>
    </DashboardLayout>
  );
}
```

### FILE: `crime-hotspot-system/frontend/src/hooks/useAuth.ts`
```ts
import { useAuthStore } from "../store/authStore";

export default function useAuth() {
  const token = useAuthStore((s) => s.token);
  const user = useAuthStore((s) => s.user);
  const logout = useAuthStore((s) => s.logout);
  return { token, user, logout, isAuthenticated: Boolean(token) };
}
```

### FILE: `crime-hotspot-system/frontend/src/animations/pageVariants.ts`
```ts
export const pageVariants = {
  initial: { opacity: 0, y: 8 },
  animate: { opacity: 1, y: 0 },
  exit: { opacity: 0, y: -8 },
};
```

---

## APIs

- `POST /api/v1/auth/register`
- `POST /api/v1/auth/login`
- `GET /api/v1/crime/stats`
- `POST /api/v1/crime/upload` (CSV upload)
- `GET /api/v1/analysis/hotspots?algorithm=kmeans|dbscan`
- `GET /api/v1/analysis/prediction`
- `GET /api/v1/admin/users`
- `GET /api/v1/admin/logs`
- `GET /api/v1/admin/overview`
- `GET /health`

---

## Installation Steps

### 1) Open in VS Code

Open folder: `crime-hotspot-system`

### 2) Backend Setup

```bash
cd backend
python -m venv .venv
# Windows:
.venv\Scripts\activate
# Linux/macOS:
# source .venv/bin/activate
pip install -r requirements.txt
copy .env.example .env   # Windows
# cp .env.example .env   # Linux/macOS
```

### 3) PostgreSQL + Redis Setup

Option A (recommended):
```bash
cd ..
docker compose up -d postgres redis
```

Option B (manual):
- PostgreSQL DB: `crime_db`
- User: `crime_user`
- Password: `crime_pass`
- Redis at `localhost:6379`

### 4) Run Backend

```bash
cd backend
uvicorn app.main:app --reload --port 8000
```

### 5) Frontend Setup

```bash
cd ../frontend
npm install
# optional:
copy .env.example .env
npm run dev
```

---

## Run Commands

### Backend
```bash
cd backend
uvicorn app.main:app --reload --port 8000
```

### Frontend
```bash
cd frontend
npm run dev
```

### Docker Full Stack
```bash
docker compose up --build
```

---

## VS Code Exact Terminal Plan

- **Terminal 1** (infra):  
  `docker compose up -d postgres redis`
- **Terminal 2** (backend):  
  `cd backend`  
  `.\.venv\Scripts\activate`  
  `uvicorn app.main:app --reload --port 8000`
- **Terminal 3** (frontend):  
  `cd frontend`  
  `npm run dev`

Then open:
- Frontend: `http://localhost:5173`
- Swagger: `http://localhost:8000/docs`

---

If you want, next I can generate a sample `datasets/chicago_crime.csv` starter file with valid column headers + sample rows so upload works immediately.
